package com.example.hudensemble.hudens;

/**
 * Shared UI selectors and ids used by HudEnsemble.
 */
public final class HudEnsembleUi {

    /** Root group id inside the UI tree (defined in Common/UI/Custom/HUD/HudEnsemble.ui). */
    public static final String ROOT_GROUP_ID = "HudEnsemble";

    /** Selector for the root group. */
    public static final String ROOT_SELECTOR = "#" + ROOT_GROUP_ID;

    private HudEnsembleUi() {}
}
